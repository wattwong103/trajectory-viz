/**
 * Vitest setup — runs once before any test file.
 *
 * @testing-library/jest-dom registers DOM matchers like
 * .toBeInTheDocument(), .toHaveTextContent(), etc.
 */
import '@testing-library/jest-dom';
